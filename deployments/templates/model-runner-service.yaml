apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.salesModelRunner.name }}-service
spec:
  selector:
    app: {{ .Values.salesModelRunner.name }}
    tier: backend
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP